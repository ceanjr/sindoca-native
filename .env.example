# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Admin Email (for workspace invites)
EXPO_PUBLIC_ADMIN_EMAIL=admin@example.com

# API URL (for Spotify integration and other backend calls)
# ⚠️ IMPORTANTE: NÃO use localhost ou 127.0.0.1 - não funciona no React Native!
#
# DESENVOLVIMENTO (WSL):
#   1. Rode: ./scripts/get-windows-ip.sh para descobrir o IP do Windows
#   2. Use o IP do Windows (ex: http://192.168.1.100:3000)
#   3. Ou descubra manualmente: No Windows PowerShell rode 'ipconfig'
#
# DESENVOLVIMENTO (macOS/Linux):
#   - Use seu IP local: System Preferences > Network > Advanced > TCP/IP
#
# PRODUÇÃO:
#   - Use a URL do seu backend deployado (Vercel, Railway, etc.)
#   - Configure no EAS Build: eas secret:create --scope project --name EXPO_PUBLIC_API_URL
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000

# Spotify API (opcional)
EXPO_PUBLIC_SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret

# IMPORTANTE:
# 1. Copie este arquivo para .env
# 2. Preencha com suas credenciais reais
# 3. Nunca commite o arquivo .env (já está no .gitignore)
# 4. Para produção, use variáveis de ambiente do EAS Build
